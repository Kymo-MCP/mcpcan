# Release Notes

**Scope**: `v2.1.2` ... `HEAD`

## 🐛 Bug Fixes
- **Hardened proxy instance handling and deployment defaults**: Fixed proxy instance handling paths and improved deployment defaults to reduce protocol/environment drift during instance create and update flows. (`1ea2690`)
- **Fixed OpenAPI import and publishing policy flow**: Improved the OpenAPI import pipeline and corrected CCR publishing policy behavior for more reliable import and release consistency. (`1e1ccce`)

## 📚 Documentation
- **Removed outdated v1.x caution text**: Cleaned obsolete v1.x warning statements from both Chinese and English README files. (`25ee535`)
- **Refreshed release note documents**: Updated bilingual release notes covering changes from `v2.1.2` to the current branch. (`ad46db3`)

## 🔧 Chore
- **Prepared v2.1.3 patch release**: Finalized version and configuration updates required for the v2.1.3 patch release process. (`5a1de6e`)
