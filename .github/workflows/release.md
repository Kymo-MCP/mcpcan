# Release Notes

**Changes**: `v2.1.2` ... `fix/v2.1.3`

---

## 🐛 Bug Fixes

- **OpenAPI Import Flow Hardening**: Improved OpenAPI import handling in backend/frontend integration to reduce parsing and dialog workflow inconsistencies in MCP instance import scenarios. (`1e1ccce`)

---

## 🏗️ Build & Release

- **CCR Publishing Policy Update**: Updated build/push policy so CCR publishing follows amd64-only constraints while Docker Hub keeps multi-arch support where required. (`1e1ccce`)

- **v2.1.3 Release Preparation**: Release preparation and branch stabilization updates for v2.1.3. (`5a1de6e`)
